# docker-wordpress
  docker example to init future project
  - requests:
    - docker
    - docker-commpose
  - how use:
    copy files and execute:
    
    ```
      docker-compose up
     ```
     or
     ```
      sudo docker-compose up
     ```
